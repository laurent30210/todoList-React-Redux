// == Import npm
import React from 'react';
// == Import
import './footer.scss';

// == Composant
const Footer = () => (
  <footer className="footer">
    <p className="footer__txt">
      Todo List created with REACT, redux (actionCreator, combineReducer) - tutorial youtube in progress
      - this todoList is intended to help people who wish to discover redux
    </p>
  </footer>
);

// == Export
export default Footer;
